#
# macOS makefile
#

CFLAGS+=`sdl-config --cflags` -D__MACOSX_CORE__ `pkg-config --cflags libusb-1.0`
PREFIX=../mac/
SUFFIX=
LIBS+=`sdl-config --libs` -framework CoreMIDI -framework CoreAudio -lpthread `pkg-config --libs libusb-1.0`

include makefile.common
